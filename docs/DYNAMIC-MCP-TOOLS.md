# MCP Tools - Встроенный сервер динамических инструментов

## Обзор идеи

**MCP Tools** — встроенный (built-in) MCP сервер, который позволяет пользователям создавать динамические инструменты, промпты и ресурсы без установки внешних пакетов.

### Ключевые характеристики

| Аспект | Описание |
|--------|----------|
| **Endpoint** | `localhost:4040/mcp-tools` (часть основного хоста) |
| **Тип** | Встроенный, не внешний процесс |
| **Удаляемость** | Нельзя удалить, можно только включить/выключить |
| **Управление** | Полный CRUD для tools/prompts/resources через UI |

### Отличия от обычных серверов

```
┌─────────────────────────────────────────────────────────────────┐
│  Обычные MCP серверы                                            │
├─────────────────────────────────────────────────────────────────┤
│  - Внешние процессы (spawn)                                     │
│  - Свой порт (4100-4999)                                        │
│  - stdio транспорт                                              │
│  - Можно удалить                                                │
│  - npx/pnpx/yarn/bunx/local                                     │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  MCP Tools (встроенный)                                         │
├─────────────────────────────────────────────────────────────────┤
│  - Часть основного процесса McpHost                             │
│  - Endpoint: /mcp-tools на порту 4040                           │
│  - HTTP транспорт (SSE)                                        │
│  - Нельзя удалить, только toggle                                │
│  - Динамические сущности через UI                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## Архитектура

### Общая схема

```
┌───────────────────────────────────────────────────────────────────┐
│                     McpHost (порт 4040)                            │
├───────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────────┐  ┌──────────────────┐  ┌─────────────────┐ │
│  │   API Routes      │  │   MCP Gateway     │  │  MCP Tools      │ │
│  │   /api/*         │  │   /mcp/:id/:ws    │  │  /mcp-tools/*  │ │
│  └──────────────────┘  └──────────────────┘  └─────────────────┘ │
│                                                        │          │
│                                                        ▼          │
│                                              ┌─────────────────┐ │
│                                              │ DynamicMcpServer│ │
│                                              │ ├─ tools[]      │ │
│                                              │ ├─ prompts[]    │ │
│                                              │ └─ resources[]  │ │
│                                              └─────────────────┘ │
│                                                        │          │
│                                                        ▼          │
│                                              ┌─────────────────┐ │
│                                              │ ToolsStore      │ │
│                                              │ mcp-tools.json  │ │
│                                              └─────────────────┘ │
└───────────────────────────────────────────────────────────────────┘
```

### Компоненты системы

```
src/host/
├── mcp-tools/
│   ├── index.ts                    # DynamicMcpServer - основной класс
│   ├── types.ts                    # Типы для динамических сущностей
│   ├── handlers/
│   │   ├── tools.ts                # Обработчики tools
│   │   ├── prompts.ts              # Обработчики prompts
│   │   └── resources.ts            # Обработчики resources
│   ├── executors/
│   │   ├── static.ts               # Статический результат
│   │   ├── http.ts                 # HTTP запрос
│   │   └── function.ts             # JavaScript функция
│   └── validation/
│       └── schema.ts               # Валидация JSON Schema

src/host/stores/
└── ToolsStore.ts                   # Хранилище mcp-tools.json

src/renderer/components/
├── mcp-tools/
│   ├── McpToolsPage.tsx            # Главная страница
│   ├── tabs/
│   │   ├── ToolsTab.tsx            # Вкладка инструментов
│   │   ├── PromptsTab.tsx          # Вкладка промптов
│   │   └── ResourcesTab.tsx        # Вкладка ресурсов
│   ├── editors/
│   │   ├── ToolEditor.tsx          # Редактор инструмента
│   │   ├── PromptEditor.tsx        # Редактор промпта
│   │   └── ResourceEditor.tsx      # Редактор ресурса
│   ├── forms/
│   │   ├── StaticForm.tsx          # Форма статического результата
│   │   ├── HttpForm.tsx            # Форма HTTP запроса
│   │   └── FunctionForm.tsx        # Форма JavaScript функции
│   └── validation/
│       └── SchemaEditor.tsx        # Редактор JSON Schema с валидацией
```

---

## Модели данных

### Типы сущностей

```typescript
// ============================================================================
// Base Types
// ============================================================================

type EntityType = 'tool' | 'prompt' | 'resource';
type ExecutorType = 'static' | 'http' | 'function';

interface BaseEntity {
  id: string;
  name: string;                     // Уникальное имя (snake_case)
  description: string;
  enabled: boolean;
  createdAt: number;
  updatedAt: number;
}

// ============================================================================
// Tool Types
// ============================================================================

interface DynamicTool extends BaseEntity {
  type: 'tool';
  inputSchema: JsonSchema;          // JSON Schema для параметров
  executor: ToolExecutor;
}

type ToolExecutor =
  | StaticExecutor
  | HttpExecutor
  | FunctionExecutor;

interface StaticExecutor {
  type: 'static';
  content: string;                  // Статический контент (текст/JSON)
  contentType: 'text' | 'json';
}

interface HttpExecutor {
  type: 'http';
  method: 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';
  url: string;                      // URL с поддержкой {{param}} плейсхолдеров
  headers?: Record<string, string>; // Заголовки (могут содержать {{param}})
  body?: string;                    // Тело запроса (для POST/PUT/PATCH)
  bodyType: 'json' | 'form' | 'text';
  timeout?: number;                 // Таймаут в мс (default: 30000)
  // Маппинг ответа
  responseMapping?: {
    successPath?: string;           // JSONPath для извлечения результата
    errorPath?: string;             // JSONPath для извлечения ошибки
  };
}

interface FunctionExecutor {
  type: 'function';
  code: string;                     // JavaScript код функции
  // Функция получает: (params: Record<string, unknown>, context: ExecutionContext) => Promise<unknown>
}

// ============================================================================
// Prompt Types
// ============================================================================

interface DynamicPrompt extends BaseEntity {
  type: 'prompt';
  arguments?: PromptArgument[];     // Аргументы промпта
  template: string;                 // Шаблон с {{arg}} плейсхолдерами
}

interface PromptArgument {
  name: string;
  description?: string;
  required?: boolean;
}

// ============================================================================
// Resource Types
// ============================================================================

interface DynamicResource extends BaseEntity {
  type: 'resource';
  uri: string;                      // URI ресурса (например: file:///path или custom://...)
  mimeType?: string;                // MIME тип (default: text/plain)
  executor: ResourceExecutor;
}

type ResourceExecutor =
  | StaticExecutor
  | HttpExecutor
  | FunctionExecutor;

// ============================================================================
// Execution Context
// ============================================================================

interface ExecutionContext {
  // Информация о сессии
  session: {
    workspaceId: string;
    projectRoot?: string;
    clientType?: string;
  };
  // Метаданные запроса
  request: {
    timestamp: number;
    requestId: string;
  };
  // Доступные утилиты
  utils: {
    fetch: typeof fetch;            // Для HTTP запросов
    log: (message: string) => void; // Логирование
  };
}

// ============================================================================
// Store Types
// ============================================================================

interface McpToolsConfig {
  enabled: boolean;                 // Глобальный toggle
  tools: DynamicTool[];
  prompts: DynamicPrompt[];
  resources: DynamicResource[];
}
```

### JSON Schema для валидации

```typescript
// Пример inputSchema для инструмента
const weatherToolSchema: JsonSchema = {
  type: 'object',
  properties: {
    city: {
      type: 'string',
      description: 'Название города'
    },
    units: {
      type: 'string',
      enum: ['celsius', 'fahrenheit'],
      default: 'celsius'
    }
  },
  required: ['city']
};
```

---

## API Endpoints

### Management API

```
# Глобальное управление MCP Tools
GET    /api/mcp-tools/status         # Статус (enabled, counts)
POST   /api/mcp-tools/enable         # Включить MCP Tools
POST   /api/mcp-tools/disable        # Выключить MCP Tools

# CRUD для Tools
GET    /api/mcp-tools/tools          # Список всех tools
POST   /api/mcp-tools/tools          # Создать tool
GET    /api/mcp-tools/tools/:id      # Получить tool
PUT    /api/mcp-tools/tools/:id      # Обновить tool
DELETE /api/mcp-tools/tools/:id      # Удалить tool
POST   /api/mcp-tools/tools/:id/toggle  # Включить/выключить
POST   /api/mcp-tools/tools/:id/test    # Тестировать tool

# CRUD для Prompts
GET    /api/mcp-tools/prompts        # Список всех prompts
POST   /api/mcp-tools/prompts        # Создать prompt
GET    /api/mcp-tools/prompts/:id    # Получить prompt
PUT    /api/mcp-tools/prompts/:id    # Обновить prompt
DELETE /api/mcp-tools/prompts/:id    # Удалить prompt
POST   /api/mcp-tools/prompts/:id/toggle  # Включить/выключить

# CRUD для Resources
GET    /api/mcp-tools/resources      # Список всех resources
POST   /api/mcp-tools/resources      # Создать resource
GET    /api/mcp-tools/resources/:id  # Получить resource
PUT    /api/mcp-tools/resources/:id  # Обновить resource
DELETE /api/mcp-tools/resources/:id  # Удалить resource
POST   /api/mcp-tools/resources/:id/toggle  # Включить/выключить

# Валидация
POST   /api/mcp-tools/validate/schema     # Валидировать JSON Schema
POST   /api/mcp-tools/validate/function   # Валидировать JS функцию
```

### MCP Protocol Endpoint

```
# SSE endpoint для MCP протокола
GET/POST /mcp-tools                  # MCP SSE endpoint

# Или через стандартный gateway (если нужно)
POST /mcp/mcp-tools/:workspaceId    # Через gateway
```

---

## UI Design

### Общий вид страницы MCP Tools

```
┌─────────────────────────────────────────────────────────────────────────┐
│  MCP Tools                                              [Toggle: ON/OFF] │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────────┬──────────────┬──────────────┐                          │
│  │   Tools     │   Prompts    │  Resources   │                          │
│  │   (12)      │   (5)        │  (3)         │                          │
│  └─────────────┴──────────────┴──────────────┘                          │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                                                         [+ Add Tool]│ │
│  │  ┌──────────────────────────────────────────────────────────────┐  │ │
│  │  │ 🟢 get_weather                                    [Edit] [⋮] │  │ │
│  │  │ Получает текущую погоду для указанного города                │  │ │
│  │  │ Type: HTTP • Params: 2 • Last used: 2 min ago                │  │ │
│  │  └──────────────────────────────────────────────────────────────┘  │ │
│  │                                                                      │ │
│  │  ┌──────────────────────────────────────────────────────────────┐  │ │
│  │  │ 🔴 translate_text                                  [Edit] [⋮]│  │ │
│  │  │ Переводит текст с одного языка на другой                     │  │ │
│  │  │ Type: Function • Params: 3 • Disabled                        │  │ │
│  │  └──────────────────────────────────────────────────────────────┘  │ │
│  │                                                                      │ │
│  │  ┌──────────────────────────────────────────────────────────────┐  │ │
│  │  │ 🟢 get_system_info                                 [Edit] [⋮]│  │ │
│  │  │ Возвращает информацию о системе                              │  │ │
│  │  │ Type: Static • Params: 0                                     │  │ │
│  │  └──────────────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### Редактор инструмента (Tool Editor)

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Edit Tool: get_weather                              [Test] [Save] [✕]  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─ Basic Info ────────────────────────────────────────────────────────┐│
│  │                                                                      ││
│  │  Name*            [get_weather                    ] ✓ Valid          ││
│  │  Description*     [Получает текущую погоду для указанного города   ]││
│  │  Enabled          [✓]                                                ││
│  │                                                                      ││
│  └──────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│  ┌─ Input Schema (JSON Schema) ────────────────────────────────────────┐│
│  │  ┌────────────────────────────────────────────────────────────────┐ ││
│  │  │ {                                                              │ ││
│  │  │   "type": "object",                                            │ ││
│  │  │   "properties": {                                              │ ││
│  │  │     "city": {                                                  │ ││
│  │  │       "type": "string",                                        │ ││
│  │  │       "description": "Название города"                         │ ││
│  │  │     },                                                         │ ││
│  │  │     "units": {                                                 │ ││
│  │  │       "type": "string",                                        │ ││
│  │  │       "enum": ["celsius", "fahrenheit"],                       │ ││
│  │  │       "default": "celsius"                                     │ ││
│  │  │     }                                                          │ ││
│  │  │   },                                                           │ ││
│  │  │   "required": ["city"]                                         │ ││
│  │  │ }                                                              │ ││
│  │  └────────────────────────────────────────────────────────────────┘ ││
│  │  [✓ Schema is valid]                                                 ││
│  └──────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│  ┌─ Executor Type ─────────────────────────────────────────────────────┐│
│  │                                                                      ││
│  │  (○) Static   (●) HTTP Request   (○) Function                       ││
│  │                                                                      ││
│  └──────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│  ┌─ HTTP Configuration ────────────────────────────────────────────────┐│
│  │                                                                      ││
│  │  Method    [POST ▼]                                                  ││
│  │  URL*      [https://api.weather.com/v1/current?city={{city}}       ]││
│  │                                                                      ││
│  │  Headers                                                [+ Add]      ││
│  │  ┌──────────────────────────────────────────────────────────────┐   ││
│  │  │  Authorization    [Bearer {{API_KEY}}              ] [✕]     │   ││
│  │  │  Content-Type     [application/json                ] [✕]     │   ││
│  │  └──────────────────────────────────────────────────────────────┘   ││
│  │                                                                      ││
│  │  Body (JSON)                                                         ││
│  │  ┌────────────────────────────────────────────────────────────────┐ ││
│  │  │ {                                                              │ ││
│  │  │   "city": "{{city}}",                                          │ ││
│  │  │   "units": "{{units}}"                                         │ ││
│  │  │ }                                                              │ ││
│  │  └────────────────────────────────────────────────────────────────┘ ││
│  │                                                                      ││
│  │  Timeout (ms)  [30000        ]                                       ││
│  │                                                                      ││
│  │  Response Mapping (optional)                                         ││
│  │  Success Path  [$.data.weather     ]  (JSONPath)                     ││
│  │  Error Path    [$.error.message    ]                                 ││
│  │                                                                      ││
│  └──────────────────────────────────────────────────────────────────────┘│
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### Function Executor Editor

```
┌─────────────────────────────────────────────────────────────────────────┐
│  ┌─ Function Code ─────────────────────────────────────────────────────┐│
│  │                                                                      ││
│  │  // Function signature:                                              ││
│  │  // async (params, context) => result                                ││
│  │                                                                      ││
│  │  ┌────────────────────────────────────────────────────────────────┐ ││
│  │  │ async (params, context) => {                                   │ ││
│  │  │   const { city, units } = params;                              │ ││
│  │  │   const { fetch, log } = context.utils;                        │ ││
│  │  │                                                                │ ││
│  │  │   log(`Fetching weather for ${city}...`);                      │ ││
│  │  │                                                                │ ││
│  │  │   const response = await fetch(                                │ ││
│  │  │     `https://api.weather.com/v1/current?city=${city}`          │ ││
│  │  │   );                                                           │ ││
│  │  │   const data = await response.json();                          │ ││
│  │  │                                                                │ ││
│  │  │   return {                                                     │ ││
│  │  │     temperature: data.temp,                                    │ ││
│  │  │     description: data.weather[0].description,                  │ ││
│  │  │     humidity: data.humidity                                    │ ││
│  │  │   };                                                           │ ││
│  │  │ }                                                              │ ││
│  │  └────────────────────────────────────────────────────────────────┘ ││
│  │                                                                      ││
│  │  [✓ Syntax is valid]                                                 ││
│  │                                                                      ││
│  │  ┌─ Available in Context ──────────────────────────────────────────┐││
│  │  │ params: { city: string, units: string }  // From inputSchema    │││
│  │  │ context.session.workspaceId: string                             │││
│  │  │ context.session.projectRoot: string | undefined                 │││
│  │  │ context.request.timestamp: number                               │││
│  │  │ context.request.requestId: string                               │││
│  │  │ context.utils.fetch: (url, options?) => Promise                 │││
│  │  │ context.utils.log: (message) => void                            │││
│  │  └─────────────────────────────────────────────────────────────────┘││
│  │                                                                      ││
│  └──────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘
```

### Test Panel

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Test Tool: get_weather                                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─ Input Parameters ──────────────────────────────────────────────────┐│
│  │                                                                      ││
│  │  city*     [Moscow                    ]                              ││
│  │  units     [celsius        ▼]                                        ││
│  │                                                                      ││
│  │                                                           [Run Test] ││
│  └──────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│  ┌─ Result ────────────────────────────────────────────────────────────┐│
│  │  ✓ Success (245ms)                                                   ││
│  │                                                                      ││
│  │  {                                                                   ││
│  │    "temperature": 15,                                                ││
│  │    "description": "Partly cloudy",                                   ││
│  │    "humidity": 65                                                    ││
│  │  }                                                                   ││
│  │                                                                      ││
│  └──────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│  ┌─ Logs ──────────────────────────────────────────────────────────────┐│
│  │  [INFO] Fetching weather for Moscow...                               ││
│  │  [DEBUG] HTTP GET https://api.weather.com/v1/current?city=Moscow     ││
│  │  [DEBUG] Response status: 200                                        ││
│  └──────────────────────────────────────────────────────────────────────┘│
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Валидация

### Уникальность имён (Name Uniqueness)

**Правило**: Имена уникальны **в рамках своего типа сущности**.

```
┌─────────────────────────────────────────────────────────────────────────┐
│  NAMESPACE ISOLATION                                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  Tools namespace:      [get_weather] [translate] [search]               │
│  Prompts namespace:    [get_weather] [review]    [explain]   ✓ OK       │
│  Resources namespace:  [get_weather] [readme]    [config]               │
│                                                                          │
│  Одинаковое имя "get_weather" в разных типах — РАЗРЕШЕНО                │
│  Одинаковое имя "get_weather" в двух tools — ЗАПРЕЩЕНО                  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

**Почему так?**

MCP протокол использует разные методы для разных типов:
- `tools/list` → возвращает только tools
- `prompts/list` → возвращает только prompts
- `resources/list` → возвращает только resources

Клиент (Claude, Cursor) получает их в разных списках, поэтому коллизий нет.

**Валидация при создании/редактировании:**

```typescript
// Проверка уникальности имени
function validateUniqueName(
  name: string,
  entityType: 'tool' | 'prompt' | 'resource',
  excludeId?: string  // При редактировании — исключаем текущую сущность
): ValidationResult {
  const store = getToolsStore();

  // Получаем только сущности того же типа
  const entities = entityType === 'tool'
    ? store.tools
    : entityType === 'prompt'
      ? store.prompts
      : store.resources;

  // Проверяем уникальность в рамках типа
  const duplicate = entities.find(e =>
    e.name === name && e.id !== excludeId
  );

  if (duplicate) {
    return {
      valid: false,
      error: `${entityType} with name "${name}" already exists`
    };
  }

  return { valid: true };
}
```

**UI индикация:**

```
Name: [get_weather    ] ✓   # Уникально среди tools
Name: [get_weather    ] ✗   # "Tool with name 'get_weather' already exists"
```

---

### Уровни валидации

```
┌────────────────────────────────────────────────────────────────────────┐
│                         VALIDATION LAYERS                               │
├────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. UI Level (Real-time)                                                │
│     ├─ Name: snake_case, non-empty                                      │
│     ├─ Name: unique within entity type (tools/prompts/resources)        │
│     ├─ Description: non-empty                                           │
│     ├─ JSON Schema: valid JSON, valid schema structure                  │
│     ├─ URL: valid URL format                                            │
│     └─ Function: valid JS syntax                                        │
│                                                                         │
│  2. API Level (On Save)                                                 │
│     ├─ All UI validations                                               │
│     ├─ Name uniqueness check (within type)                              │
│     ├─ JSON Schema compilation (Ajv)                                    │
│     ├─ Function sandbox test                                            │
│     └─ HTTP URL reachability (optional)                                 │
│                                                                         │
│  3. Runtime Level (On Execute)                                          │
│     ├─ Input params validation against schema                           │
│     ├─ HTTP response handling                                           │
│     ├─ Function execution sandbox                                       │
│     └─ Timeout enforcement                                              │
│                                                                         │
└────────────────────────────────────────────────────────────────────────┘
```

### Индикаторы валидации в UI

```
Name:        [get_weather        ] ✓    # Зеленая галочка = валидно
Name:        [GetWeather         ] ✗    # Красный крестик = ошибка
                                  └── "Name must be snake_case"

Schema:      [{ "type": "obj... ] ⚠    # Желтый треугольник = warning
                                  └── "Schema has no required fields"

Function:    [async (params)... ] ✓    # Syntax valid
             [async (params)... ] ✗    # Syntax error at line 5
                                  └── "Unexpected token }"
```

---

## Безопасность

### Песочница для Function Executor

```typescript
// Безопасное выполнение пользовательских функций
class FunctionSandbox {
  private vm: VM2;  // Используем vm2 для изоляции

  async execute(
    code: string,
    params: Record<string, unknown>,
    context: ExecutionContext
  ): Promise<unknown> {
    const sandbox = {
      params,
      context: this.sanitizeContext(context),
      console: {
        log: context.utils.log,
        warn: context.utils.log,
        error: context.utils.log,
      },
      fetch: this.createSafeFetch(context),
      // Безопасные встроенные объекты
      JSON,
      Date,
      Math,
      Array,
      Object,
      String,
      Number,
      Boolean,
      // НЕ доступны: require, process, fs, child_process и т.д.
    };

    return this.vm.run(`
      (async () => {
        const fn = ${code};
        return await fn(params, context);
      })()
    `, sandbox);
  }

  private createSafeFetch(context: ExecutionContext) {
    // Fetch с ограничениями:
    // - Только HTTP/HTTPS
    // - Таймаут
    // - Логирование
    return async (url: string, options?: RequestInit) => {
      context.utils.log(`HTTP ${options?.method || 'GET'} ${url}`);
      // ... реализация с ограничениями
    };
  }
}
```

### Ограничения

| Ограничение | Значение | Описание |
|-------------|----------|----------|
| Timeout | 30s | Максимальное время выполнения |
| Memory | 128MB | Лимит памяти для функции |
| Network | HTTP/HTTPS only | Разрешенные протоколы |
| File System | None | Нет доступа к файловой системе |
| Child Process | None | Нет запуска процессов |

---

## Хранение данных

### Файл конфигурации

```
~/.config/mcp-manager/config/
├── servers.json          # Существующие серверы
├── workspaces.json       # Workspaces
└── mcp-tools.json        # NEW: Динамические инструменты
```

### Структура mcp-tools.json

```json
{
  "enabled": true,
  "tools": [
    {
      "id": "uuid-1",
      "name": "get_weather",
      "description": "Получает текущую погоду для указанного города",
      "enabled": true,
      "inputSchema": {
        "type": "object",
        "properties": {
          "city": { "type": "string", "description": "Название города" },
          "units": { "type": "string", "enum": ["celsius", "fahrenheit"] }
        },
        "required": ["city"]
      },
      "executor": {
        "type": "http",
        "method": "GET",
        "url": "https://api.weather.com/v1/current?city={{city}}&units={{units}}",
        "headers": {
          "Authorization": "Bearer {{API_KEY}}"
        },
        "timeout": 30000
      },
      "createdAt": 1702000000000,
      "updatedAt": 1702000000000
    }
  ],
  "prompts": [
    {
      "id": "uuid-2",
      "name": "code_review",
      "description": "Промпт для code review",
      "enabled": true,
      "arguments": [
        { "name": "language", "description": "Язык программирования", "required": true },
        { "name": "focus", "description": "На что обратить внимание" }
      ],
      "template": "Please review the following {{language}} code...",
      "createdAt": 1702000000000,
      "updatedAt": 1702000000000
    }
  ],
  "resources": [
    {
      "id": "uuid-3",
      "name": "project_readme",
      "description": "README проекта",
      "enabled": true,
      "uri": "project://readme",
      "mimeType": "text/markdown",
      "executor": {
        "type": "function",
        "code": "async (params, context) => { ... }"
      },
      "createdAt": 1702000000000,
      "updatedAt": 1702000000000
    }
  ]
}
```

---

## MCP Protocol Implementation

### Поддерживаемые методы

```typescript
// Tools
'tools/list'     -> Возвращает список enabled tools с их inputSchema
'tools/call'     -> Вызывает tool с параметрами, возвращает результат

// Prompts
'prompts/list'   -> Возвращает список enabled prompts с их arguments
'prompts/get'    -> Возвращает prompt template с подставленными аргументами

// Resources
'resources/list' -> Возвращает список enabled resources
'resources/read' -> Читает содержимое ресурса
```

### Пример tools/list response

```json
{
  "tools": [
    {
      "name": "get_weather",
      "description": "Получает текущую погоду для указанного города",
      "inputSchema": {
        "type": "object",
        "properties": {
          "city": { "type": "string", "description": "Название города" },
          "units": { "type": "string", "enum": ["celsius", "fahrenheit"] }
        },
        "required": ["city"]
      }
    }
  ]
}
```

---

## План реализации

### Фаза 1: Backend Core (Приоритет: Высокий)

```
□ 1.1 Создать типы (src/host/mcp-tools/types.ts)
    - DynamicTool, DynamicPrompt, DynamicResource
    - ExecutorType, ExecutionContext
    - Validation types

□ 1.2 Создать ToolsStore (src/host/stores/ToolsStore.ts)
    - CRUD операции для tools/prompts/resources
    - Загрузка/сохранение mcp-tools.json
    - Валидация при сохранении

□ 1.3 Создать DynamicMcpServer (src/host/mcp-tools/index.ts)
    - MCP protocol handlers
    - tools/list, tools/call
    - prompts/list, prompts/get
    - resources/list, resources/read

□ 1.4 Создать Executors
    - StaticExecutor: возврат статического контента
    - HttpExecutor: HTTP запросы с плейсхолдерами
    - FunctionExecutor: безопасное выполнение JS

□ 1.5 Интегрировать в McpHost
    - Добавить endpoint /mcp-tools
    - Добавить API routes /api/mcp-tools/*
    - Регистрация как "встроенный" сервер
```

### Фаза 2: API Routes (Приоритет: Высокий)

```
□ 2.1 Management routes
    - GET/POST /api/mcp-tools/status
    - POST /api/mcp-tools/enable|disable

□ 2.2 Tools CRUD
    - GET/POST/PUT/DELETE /api/mcp-tools/tools
    - POST /api/mcp-tools/tools/:id/toggle
    - POST /api/mcp-tools/tools/:id/test

□ 2.3 Prompts CRUD
    - GET/POST/PUT/DELETE /api/mcp-tools/prompts
    - POST /api/mcp-tools/prompts/:id/toggle

□ 2.4 Resources CRUD
    - GET/POST/PUT/DELETE /api/mcp-tools/resources
    - POST /api/mcp-tools/resources/:id/toggle

□ 2.5 Validation endpoints
    - POST /api/mcp-tools/validate/schema
    - POST /api/mcp-tools/validate/function
```

### Фаза 3: Frontend UI (Приоритет: Высокий)

```
□ 3.1 Добавить MCP Tools в список серверов
    - Отображать как первый "встроенный" сервер
    - Особая иконка/стиль
    - Нельзя удалить, только toggle

□ 3.2 Создать McpToolsPage
    - Tabs: Tools | Prompts | Resources
    - Список сущностей с поиском/фильтрацией
    - Quick actions (toggle, edit, delete)

□ 3.3 Создать ToolEditor
    - Basic info form
    - JSON Schema editor с валидацией
    - Executor type selector
    - Static/HTTP/Function forms

□ 3.4 Создать PromptEditor
    - Basic info form
    - Arguments list editor
    - Template editor с подсветкой {{arg}}

□ 3.5 Создать ResourceEditor
    - Basic info form
    - URI input
    - MIME type selector
    - Executor form

□ 3.6 Добавить валидацию
    - Real-time validation indicators
    - Schema validation feedback
    - Syntax highlighting для JSON/JS
```

### Фаза 4: Testing & Polish (Приоритет: Средний)

```
□ 4.1 Test Panel
    - UI для тестирования tools
    - Input form генерируется из schema
    - Отображение результата и логов

□ 4.2 Import/Export
    - Экспорт tool/prompt/resource в JSON
    - Импорт из JSON
    - Валидация при импорте

□ 4.3 Templates
    - Предустановленные шаблоны tools
    - Quick start для новых пользователей

□ 4.4 Documentation
    - Inline help в UI
    - Examples для каждого типа executor
```

---

## Интеграция с существующей архитектурой

### Управление включением/выключением

**Только глобальный toggle** — без per-workspace control для отдельных сущностей.

```
┌─────────────────────────────────────────────────────────────────────────┐
│  УРОВНИ УПРАВЛЕНИЯ                                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  1. MCP Tools Server toggle (глобально)                                  │
│     └─ Включает/выключает весь сервер для ВСЕХ workspaces               │
│                                                                          │
│  2. Per-entity toggle (внутри сервера)                                   │
│     └─ Tool "get_weather": [enabled ✓]  — доступен везде                │
│     └─ Tool "translate":   [enabled ✗]  — отключен везде                │
│                                                                          │
│  3. Per-workspace server toggle (как у обычных серверов)                 │
│     └─ Workspace A: MCP Tools [enabled ✓] → проксируется                │
│     └─ Workspace B: MCP Tools [enabled ✗] → не проксируется             │
│                                                                          │
│  НЕТ per-workspace per-entity toggle — слишком сложно                   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

**Логика:**
- Хотите отключить tool глобально? → Toggle на самом tool
- Хотите отключить MCP Tools для конкретного WP? → Отключите сервер для этого WP (как обычный сервер)
- Хотите отключить конкретный tool только для одного WP? → Не поддерживается (отключите весь сервер)

### Отображение в ServerList

```typescript
// В ServerList отображаем MCP Tools как первый элемент
const allServers = [
  // Встроенный сервер MCP Tools
  {
    id: 'mcp-tools',
    displayName: 'MCP Tools',
    description: 'Dynamic tools, prompts & resources',
    isBuiltIn: true,  // Новый флаг — нельзя удалить
    status: mcpToolsEnabled ? 'running' : 'stopped',
    toolsCount: enabledToolsCount,      // Только enabled tools
    promptsCount: enabledPromptsCount,
    resourcesCount: enabledResourcesCount,
  },
  // Остальные серверы
  ...externalServers
];
```

### Модификация типов

```typescript
// Расширяем ServerInfo
interface ServerInfo extends ServerTemplate {
  // ... существующие поля
  isBuiltIn?: boolean;  // true для MCP Tools
}

// Добавляем в shared/types.ts
export interface McpToolsStatus {
  enabled: boolean;
  toolsCount: number;
  promptsCount: number;
  resourcesCount: number;
}
```

### WebSocket события

```typescript
// Новые типы событий
type McpToolsEventType =
  | 'mcp-tools-enabled'
  | 'mcp-tools-disabled'
  | 'mcp-tools-tool-created'
  | 'mcp-tools-tool-updated'
  | 'mcp-tools-tool-deleted'
  | 'mcp-tools-prompt-created'
  | 'mcp-tools-prompt-updated'
  | 'mcp-tools-prompt-deleted'
  | 'mcp-tools-resource-created'
  | 'mcp-tools-resource-updated'
  | 'mcp-tools-resource-deleted';
```

---

## Примеры использования

### Пример 1: Статический tool

```json
{
  "name": "get_company_info",
  "description": "Возвращает информацию о компании",
  "inputSchema": {
    "type": "object",
    "properties": {}
  },
  "executor": {
    "type": "static",
    "content": "{\"name\": \"Acme Corp\", \"founded\": 2020}",
    "contentType": "json"
  }
}
```

### Пример 2: HTTP tool

```json
{
  "name": "search_github",
  "description": "Поиск репозиториев на GitHub",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": { "type": "string", "description": "Поисковый запрос" },
      "limit": { "type": "number", "default": 10 }
    },
    "required": ["query"]
  },
  "executor": {
    "type": "http",
    "method": "GET",
    "url": "https://api.github.com/search/repositories?q={{query}}&per_page={{limit}}",
    "headers": {
      "Accept": "application/vnd.github.v3+json"
    },
    "responseMapping": {
      "successPath": "$.items"
    }
  }
}
```

### Пример 3: Function tool

```json
{
  "name": "calculate_hash",
  "description": "Вычисляет хэш строки",
  "inputSchema": {
    "type": "object",
    "properties": {
      "text": { "type": "string" },
      "algorithm": {
        "type": "string",
        "enum": ["md5", "sha256"],
        "default": "sha256"
      }
    },
    "required": ["text"]
  },
  "executor": {
    "type": "function",
    "code": "async (params, context) => { const { text, algorithm } = params; const crypto = await import('crypto'); return crypto.createHash(algorithm).update(text).digest('hex'); }"
  }
}
```

---

## Критические решения

### 1. Sandbox для функций

**Решение**: Использовать `vm2` или `isolated-vm` для безопасного выполнения пользовательского кода.

**Альтернативы**:
- `vm` module (небезопасен)
- WebWorker (ограничен в Node.js)
- Отдельный процесс (сложнее, но безопаснее)

### 2. Хранение секретов в HTTP executor

**Решение**: Использовать синтаксис `{{SECRET_NAME}}` который резолвится из SecretStore при выполнении.

### 3. MCP Protocol transport

**Решение**: Использовать SSE transport через endpoint `/mcp-tools` на основном порту 4040.

### 4. Backward compatibility

**Решение**: MCP Tools является optional feature, отключен по умолчанию при первом запуске.

---

## Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Уязвимости в sandbox | Средняя | Высокое | Тщательное тестирование, ограничение API |
| Performance при большом количестве tools | Низкая | Среднее | Ленивая загрузка, кэширование |
| Сложность UI для пользователей | Средняя | Среднее | Шаблоны, примеры, inline help |
| Конфликты имен tools | Низкая | Низкое | Валидация уникальности при создании |

---

## Заключение

MCP Tools расширяет функциональность MCP Manager, позволяя пользователям создавать динамические инструменты без необходимости писать и устанавливать отдельные MCP серверы. Это особенно полезно для:

1. **Быстрого прототипирования** — создание tool за минуты
2. **Простых интеграций** — HTTP API без кода
3. **Кастомных промптов** — переиспользуемые шаблоны
4. **Динамических ресурсов** — данные из различных источников

Архитектура интегрируется в существующую систему с минимальными изменениями, используя уже имеющиеся паттерны и компоненты.
